[project]
name = "kinyo-rag-demo"
version = "0.1.0"
description = "KINYO 顧問會議 #2 — PGVector RAG vs Agentic(grep) RAG 對照 demo"
requires-python = ">=3.11"
dependencies = [
    "streamlit>=1.40",
    "openai>=1.55",
    "psycopg[binary]>=3.2",
    "pgvector>=0.3",
    "pypdf>=5.1",
    "python-dotenv>=1.0",
    "reportlab>=4.2",
]
