# 把這個檔複製成 .env，填入老師提供的 API key。
# .env 已被 gitignore，永遠不會進 git —— 不要把真實 key 貼進任何會上傳的檔案。
OPENAI_API_KEY=

# OpenAI 模型（可不改）
EMBED_MODEL=text-embedding-3-small
CHAT_MODEL=gpt-4o-mini

# PostgreSQL（對應 docker-compose.yml，一般不用改）
PGHOST=localhost
PGPORT=5433
PGDATABASE=ragdemo
PGUSER=rag
PGPASSWORD=ragpw
